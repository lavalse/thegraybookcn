---
title: Getting started
sidebar_position: 1
---

虽然成功引入了P5.js，但是明显就感觉到react的逻辑跟P5的逻辑很不相互符合。查了一下似乎直接使用canvas配合各种hook也是一个解决方案。下面尝试一下直接引入canvas。

<CanvasScene />

当然这样就更复杂，但是更直接，都是浏览器支持的操作。

当然还可以支持SVG。但是不知道svg的动画支持如何。

import CanvasScene from "./components/scene_1_1/scene";

